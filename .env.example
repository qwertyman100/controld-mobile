# Development: requests go to /api which Vite proxies to api.controld.com
# No env var needed for dev — it just works.

# Production: point to your Cloudflare Worker URL
# VITE_API_BASE_URL=https://your-worker.your-subdomain.workers.dev
